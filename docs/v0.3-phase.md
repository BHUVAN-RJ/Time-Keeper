# Time Keeper — v0.3 phase plan (dogfooding)

**Status:** **Mostly complete** (May 2026). Daily loop is shippable; only AM credit-balance line remains before calling v0.3 “closed.”

**Goal:** habits, rituals, ADHD core loop, projects, monthly recap, and weekly retrospective per [spec_v4.md](../spec_v4.md) §9.

**Next phase for new work:** finish **v0.4** (see [`v0.4-phase.md`](v0.4-phase.md)), then v0.5.

---

## 1. What shipped

| Area | Status | Where / notes |
|------|--------|----------------|
| Habits CRUD + today checklist + 14d heatmap | Shipped | `/habits`, `today-habits-panel` |
| Streak freezes + `days_hit_last_30` | Shipped | `src/lib/habits-compute.ts` |
| AM rundown (top 3, recap, calendar, commitments) | Shipped | `AmRundownModal` on `/today` |
| **Batch close unclosed days** | Shipped | `listUnclosedDaysBeforeToday`, `batchCloseUnclosedDaysAction` |
| PM / End Day (mood, notes, top 3, incomplete flow) | Shipped | `EndDayDialog` |
| Productivity score + vs 7-day avg | Shipped | `day-compute`, Today widget |
| Off-day bank + cap + nudges | Shipped | `off-day-balance`, Today v03 panel |
| Daily + weekly credit multipliers | Shipped | `end-day.ts`, `credits-bonus` |
| Variable bonus on useful stop | Shipped | `tryApplyVariableBonus` |
| **Overwork → credits + freeze bank** | Shipped | `applyOverworkForDay` on End Day; Settings slider |
| Projects CRUD + tagging + staleness + retire | Shipped | `/projects`, Today panel |
| Weekly retro on **Week** + Sunday nudge | Shipped | `WeeklyReviewPanel`, `WeeklyReviewNudgeModal` |
| Weekly commitments on Today | Shipped | `today-v03-panel` |
| Scope reality + capacity warning | Shipped | Tasks create, Today v03 |
| Month recap + Stats embed | Shipped | `/month`, `MonthRecapPanel` on `/stats` |
| **14-day score trend chart** | Shipped | `ScoreTrendChart` on `/stats` |
| Retirement patterns | Shipped | `/stats` |
| Migrations `0006`–`0008` | Shipped | habits, off-day skip, projects, scores, overwork |
| Week: category vs goals + open tasks + prep Eisenhower sort | Shipped | May 2026 polish — see [`v0.4-phase.md`](v0.4-phase.md) |
| Weekly retro: estimate trend, category totals, stale list | Shipped | `WeeklyReviewPanel` |
| Month: score line + tag/quality breakdown | Shipped | `/month` (tags section respects Settings) |

**Routes (v0.3):** `/today`, `/week`, `/tasks`, `/habits`, `/projects`, `/month`, `/categories`, `/stats`, `/settings`.

---

## 2. Intentionally not shipped (v0.3)

| Item | Notes |
|------|--------|
| Silent service-worker Pomodoro (§6.14) | Removed; focus countdown presets on Today remain |
| Daily credit soft cap | Not implemented |
| Eisenhower 2×2 board | v0.4 |
| Reminders, tags, vacations, body doubling, Eisenhower | Shipped in v0.4 — see [`v0.4-phase.md`](v0.4-phase.md) |
| Export/import, schedule proposals | v0.4 (remaining) |

---

## 3. Partial vs spec (remaining polish)

| Item | Notes |
|------|--------|
| AM: credit balance on rundown | §6.12 |
| Overwork freeze **redemption** UI | v0.5 |

---

## 4. Migrations

Run after pull:

```bash
cd web && source .env.local && npm run db:migrate
```

| Migration | Purpose |
|-----------|---------|
| `0006_habits` | habits, completions, freezes |
| `0007_habit_off_day_skip` | off-day habit skip |
| `0008_v03_part2` | projects, weekly_reviews, off_day_balance, productivity_scores, overwork_bank, preferences |

---

## 5. Key env / settings

- Overwork split: **Settings → Overwork** (`user_preferences.overwork_credits_percent`, default 50%).
- Google Calendar: unchanged from v0.2 — see [v0.2-phase.md](v0.2-phase.md) §2.

---

## 6. Handoff to v0.4

Implement **only** [spec_v4.md](../spec_v4.md) §9 v0.4 scope after dogfooding v0.3. Do not bundle v0.4 + v0.5.
