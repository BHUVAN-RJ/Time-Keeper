# Time Keeper — v0.2 phase plan (branch `v2`)

**Goal:** scheduling, the End Day loop, and basic ADHD ergonomics per [spec_v4.md](../spec_v4.md) §9.

---

## Status (shipped on `v2`)

| Area | Status |
|------|--------|
| `tasks`, `schedule_goals`, `day_status`, `daily_reviews` | Shipped |
| Migrations `0001` + `0002` | Run `cd web && npm run db:migrate` |
| Tasks CRUD + drop w/ reason | `/tasks` |
| Schedule goals per category | Categories page (daily minutes) |
| Day status + red-day + productivity score | Computed in `lib/day-compute.ts` |
| Rolling 7-day avg on Today | Primary dashboard number |
| Weekly view | `/week` |
| Off day revert | Week tab + Today (“Not an off day”) |
| Weekly Rundown + GCal read | Spec §6.17 — **not built yet** |
| Stats history (30d) | `/stats` — completed, dropped, End Day notes, time blocks |
| Link time blocks → tasks | Start from What's next / `startBlockForTaskAction` |
| Task estimate vs actual on complete | Toast on Done |
| End Day + basic PM review | Today → End Day button |
| "What's next" | Today |
| Quick add (⌘/Ctrl+K) | Global in app layout |
| Offline reads (SW cache) | `public/sw.js` v2 |

---

## Dev

```bash
cd web && npm run db:migrate && npm run dev
```

Branch: `v2` — merge to `main` when dogfood-ready.

---

## Checkpoint (pause here)

Before v0.3 / Google Calendar / Weekly Rundown Part B:

- [ ] Quality **Chores** + **Meh** (both 0.5×; chores = upkeep, meh = low-focus)
- [ ] Off day copy: **Today is an off day** + **Not an off day** revert
- [ ] Edit/manual block: toast on invalid times (no runtime crash)
- [ ] Run `npm run db:migrate` for `0003_quality_chores` (meh → chores)
- [ ] Dogfood checklist from prior session still applies
