# Time Keeper — v0.2 phase plan (branch `v2`)

**Goal:** scheduling, the End Day loop, and basic ADHD ergonomics per [spec_v4.md](../spec_v4.md) §9.

**Baseline:** v0.1 shipped on `main` (auth, categories, time blocks, Today, credits, PWA).

---

## Scope checklist

| Area | Status | Notes |
|------|--------|-------|
| `tasks` schema + migration | Shipped (initial) | `web/drizzle/0001_hesitant_thing.sql` — run `npm run db:migrate` |
| `schedule_goals` + `day_status` schema | Shipped (tables only) | Logic not wired yet |
| Tasks CRUD (estimate required) | Shipped (initial) | `/tasks` — create, complete, schedule today, drop w/ reason |
| Link time blocks → tasks | Planned | `task_id` on `time_blocks` |
| Schedule goals per category | Planned | `schedule_goals` table |
| Day status + red-day logic | Planned | `day_status` table |
| Rolling 7-day average (dashboard) | Planned | Today or `/week` |
| Weekly view | Planned | |
| End Day + basic PM review | Planned | |
| "What's next" button | Planned | ADHD feature C |
| Quick add (Cmd/Ctrl+K) | Planned | ADHD feature D |
| Graceful drop on tasks | Planned | ADHD feature K |
| Offline reads (SW) | Planned | |

---

## Out of scope (v0.3+)

Habits, AM rundown, Eisenhower drag-drop, reminders, full stats — see spec §9 v0.3–v0.4.

---

## Dev notes

- Run migrations after pulling: `cd web && npm run db:migrate`
- Branch: `v2` — merge to `main` when v0.2 is dogfood-ready.
